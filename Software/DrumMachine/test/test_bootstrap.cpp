// test_bootstrap.cpp — sanity check that the test framework links and runs.
// Will remain in place until task 02 swaps in real DrumPad tests.

#include "test_macros.h"

TEST_CASE("bootstrap_runs") {
    EXPECT_TRUE(true);
}
